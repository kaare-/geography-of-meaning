[package]
name = "sim-core"
version = "0.1.0"
edition = "2021"
description = "Core simulation engine for Geography of Meaning"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
rayon = "1"
thiserror = "1"
clap = { version = "4", features = ["derive"] }
